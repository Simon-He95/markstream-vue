import type { CheckboxInputNode, CheckboxNode, MarkdownToken } from '../../types';
export declare function parseCheckboxToken(token: MarkdownToken): CheckboxNode;
export declare function parseCheckboxInputToken(token: MarkdownToken): CheckboxInputNode;
