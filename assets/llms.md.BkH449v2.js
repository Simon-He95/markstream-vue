import{_ as o,c as d,o as t,ag as r}from"./chunks/framework.C0wiDQbp.js";const p=JSON.parse('{"title":"markstream-vue — LLM/AI Context (/llms)","description":"","frontmatter":{},"headers":[],"relativePath":"llms.md","filePath":"llms.md"}'),a={name:"llms.md"};function s(c,e,n,i,l,m){return t(),d("div",null,[...e[0]||(e[0]=[r('<h1 id="markstream-vue-—-llm-ai-context-llms" tabindex="-1">markstream-vue — LLM/AI Context (<code>/llms</code>) <a class="header-anchor" href="#markstream-vue-—-llm-ai-context-llms" aria-label="Permalink to &quot;markstream-vue — LLM/AI Context (`/llms`)&quot;">​</a></h1><p>This file is an <strong>AI-oriented map</strong> of the project + docs so a model can answer questions quickly without re-reading the whole site.</p><p>If you are an assistant answering questions about this repo:</p><ul><li>Prefer <strong>grounding</strong> in <code>docs/guide/*</code> for user-facing behavior and <code>src/exports.ts</code> for “what is exported / supported”.</li><li>When a question is ambiguous, ask <strong>one</strong> clarifying question and propose a default.</li><li>If the user reports a bug, ask for a minimal repro (the repo has a shareable test page) and link them to the troubleshooting checklist.</li></ul><hr><h2 id="_0-one-paragraph-summary" tabindex="-1">0) One-paragraph summary <a class="header-anchor" href="#_0-one-paragraph-summary" aria-label="Permalink to &quot;0) One-paragraph summary&quot;">​</a></h2><p><code>markstream-vue</code> is a Vue 3 Markdown renderer designed for <strong>streaming</strong> (AI/SSE) and <strong>large documents</strong>: it uses a streaming-friendly parser (mid-state handling for unclosed fences/math/HTML) and a renderer with <strong>virtualization</strong> and <strong>defer-until-visible</strong> for heavy nodes (Monaco/Mermaid/KaTeX). Optional heavy features are enabled via <strong>optional peer dependencies</strong> + explicit <code>enable*()</code> calls.</p><p>If you only need the parser (no Vue), use the sibling package <code>stream-markdown-parser</code> in <code>packages/markdown-parser/</code>.</p><hr><h2 id="_1-repo-layout-where-things-live" tabindex="-1">1) Repo layout (where things live) <a class="header-anchor" href="#_1-repo-layout-where-things-live" aria-label="Permalink to &quot;1) Repo layout (where things live)&quot;">​</a></h2><ul><li>Library source: <code>src/</code><ul><li>Components: <code>src/components/*/</code></li><li>Utilities: <code>src/utils/</code></li><li>Composables: <code>src/composables/</code></li><li>Workers: <code>src/workers/</code></li><li>Public exports: <code>src/exports.ts</code></li></ul></li><li>Parser-only package: <code>packages/markdown-parser/</code> (published as <code>stream-markdown-parser</code>)</li><li>Docs site (VitePress): <code>docs/</code> (Chinese under <code>docs/zh/</code>)</li><li>Demos: <code>playground/</code> (Vite), <code>playground-nuxt/</code> (Nuxt SSR)</li><li>Tests: <code>test/</code> (Vitest)</li></ul><hr><h2 id="_2-the-core-mental-model" tabindex="-1">2) The “core mental model” <a class="header-anchor" href="#_2-the-core-mental-model" aria-label="Permalink to &quot;2) The “core mental model”&quot;">​</a></h2><p>There are two layers:</p><ol><li><p><strong>Parser layer</strong> (<code>stream-markdown-parser</code>)</p><ul><li><code>getMarkdown()</code> creates a configured <code>markdown-it-ts</code> instance</li><li><code>parseMarkdownToStructure()</code> turns Markdown text into a node tree (<code>ParsedNode[]</code>)</li><li>Streaming mid-states reduce flicker (e.g. unclosed fences / <code>$$</code> / partial inline HTML)</li></ul></li><li><p><strong>Renderer layer</strong> (<code>markstream-vue</code>)</p><ul><li><code>MarkdownRender</code> (default export) renders either: <ul><li><code>content: string</code> (component parses internally), or</li><li><code>nodes: ParsedNode[]</code> (you parse externally; recommended for streaming)</li></ul></li><li>Performance tools: <ul><li><strong>virtualization</strong> (<code>maxLiveNodes</code>, <code>liveNodeBuffer</code>)</li><li><strong>batch rendering</strong> (typewriter-like “steady stream”)</li><li><strong>defer until visible</strong> for heavy nodes</li></ul></li></ul></li></ol><hr><h2 id="_3-public-api-what-s-safe-to-suggest" tabindex="-1">3) Public API (what’s safe to suggest) <a class="header-anchor" href="#_3-public-api-what-s-safe-to-suggest" aria-label="Permalink to &quot;3) Public API (what’s safe to suggest)&quot;">​</a></h2><h3 id="from-markstream-vue-vue-library" tabindex="-1">From <code>markstream-vue</code> (Vue library) <a class="header-anchor" href="#from-markstream-vue-vue-library" aria-label="Permalink to &quot;From `markstream-vue` (Vue library)&quot;">​</a></h3><ul><li>Default component: <code>MarkdownRender</code> (also referred to as <code>NodeRenderer</code>)</li><li>Parser helpers (re-exported from the parser package): <code>getMarkdown()</code>, <code>parseMarkdownToStructure()</code>, <code>setDefaultMathOptions()</code></li><li>Custom node renderers: <ul><li><code>setCustomComponents(customId, mapping)</code></li><li><code>removeCustomComponents(customId)</code></li><li><code>clearGlobalCustomComponents()</code></li></ul></li><li>Optional feature toggles: <ul><li>Mermaid: <code>enableMermaid()</code>, <code>disableMermaid()</code></li><li>KaTeX: <code>enableKatex()</code>, <code>disableKatex()</code></li></ul></li><li>Worker injection (CDN or custom workers): <ul><li>KaTeX: <code>createKaTeXWorkerFromCDN()</code>, <code>setKaTeXWorker()</code></li><li>Mermaid: <code>createMermaidWorkerFromCDN()</code>, <code>setMermaidWorker()</code></li></ul></li></ul><p>Source of truth: <code>src/exports.ts</code></p><h3 id="from-stream-markdown-parser-parser-only" tabindex="-1">From <code>stream-markdown-parser</code> (parser-only) <a class="header-anchor" href="#from-stream-markdown-parser-parser-only" aria-label="Permalink to &quot;From `stream-markdown-parser` (parser-only)&quot;">​</a></h3><ul><li><code>getMarkdown()</code>, <code>parseMarkdownToStructure()</code>, token/node transform hooks (<code>ParseOptions</code>)</li><li>Streaming mid-state rules: inline HTML suppression + auto-close, <code>final: true</code> behavior, unclosed fences/math handling</li></ul><p>Source of truth: <code>packages/markdown-parser/src/index.ts</code>, <code>docs/guide/parser-api.md</code></p><hr><h2 id="_4-common-user-workflows-answer-templates" tabindex="-1">4) Common user workflows (answer templates) <a class="header-anchor" href="#_4-common-user-workflows-answer-templates" aria-label="Permalink to &quot;4) Common user workflows (answer templates)&quot;">​</a></h2><h3 id="a-minimal-render" tabindex="-1">A) Minimal render <a class="header-anchor" href="#a-minimal-render" aria-label="Permalink to &quot;A) Minimal render&quot;">​</a></h3><p>Use when the user just wants “render a string”:</p><ul><li>Import CSS: <code>markstream-vue/index.css</code></li><li>Render: <code>&lt;MarkdownRender :content=&quot;md&quot; /&gt;</code></li></ul><p>Docs: <code>docs/guide/quick-start.md</code>, <code>docs/guide/installation.md</code></p><h3 id="b-streaming-sse-ai-chat-with-end-of-stream" tabindex="-1">B) Streaming (SSE / AI chat) with end-of-stream <a class="header-anchor" href="#b-streaming-sse-ai-chat-with-end-of-stream" aria-label="Permalink to &quot;B) Streaming (SSE / AI chat) with end-of-stream&quot;">​</a></h3><p>Use when the user is appending chunks:</p><ul><li>Keep a growing <code>buffer</code></li><li>Update nodes: <code>parseMarkdownToStructure(buffer, md, { final })</code></li><li>When stream ends: set <code>final: true</code> (or the component prop <code>final</code>) so mid-states don’t stick</li></ul><p>Docs: <code>docs/guide/parser.md</code>, <code>docs/guide/parser-api.md</code>, <code>docs/guide/performance.md</code></p><h3 id="c-enable-heavy-features-optional-peers" tabindex="-1">C) Enable heavy features (optional peers) <a class="header-anchor" href="#c-enable-heavy-features-optional-peers" aria-label="Permalink to &quot;C) Enable heavy features (optional peers)&quot;">​</a></h3><p>Use when the user asks “why Mermaid/KaTeX/Monaco doesn’t work”:</p><ul><li>Install peer + import required peer CSS (if any)</li><li>Call <code>enableMermaid()</code> / <code>enableKatex()</code> on the client</li></ul><p>Docs: <code>docs/guide/installation.md</code>, <code>docs/guide/components.md</code>, <code>docs/guide/troubleshooting.md</code></p><hr><h2 id="_5-high-signal-troubleshooting-checklist" tabindex="-1">5) High-signal troubleshooting checklist <a class="header-anchor" href="#_5-high-signal-troubleshooting-checklist" aria-label="Permalink to &quot;5) High-signal troubleshooting checklist&quot;">​</a></h2><p>When something “renders blank” or “looks wrong”, check these in order:</p><ol><li><strong>CSS order / reset</strong>: reset first, then <code>markstream-vue/index.css</code> (often inside <code>@layer components</code> for Tailwind).</li><li><strong>Optional peer installed</strong> (Mermaid/KaTeX/Monaco/Shiki).</li><li><strong>Feature enabled</strong>: <code>enableMermaid()</code> / <code>enableKatex()</code> (where applicable).</li><li><strong>Peer CSS imported</strong>: <code>katex/dist/katex.min.css</code>, <code>stream-monaco/esm/index.css</code>, Mermaid CSS (if needed).</li><li><strong>Standalone node component wrapper</strong>: if rendering a node component alone, wrap with <code>&amp;lt;div class=&quot;markstream-vue&quot;&amp;gt;...&amp;lt;/div&amp;gt;</code> so library styles apply.</li><li><strong>SSR</strong>: in Nuxt, wrap in <code>&amp;lt;client-only&amp;gt;</code> when using browser-only peers/workers.</li></ol><p>Docs: <code>docs/guide/troubleshooting.md</code>, <code>docs/nuxt-ssr.md</code>, <code>docs/guide/tailwind.md</code></p><hr><h2 id="_6-if-asked-x-open-y-fast-routing" tabindex="-1">6) “If asked X, open Y” (fast routing) <a class="header-anchor" href="#_6-if-asked-x-open-y-fast-routing" aria-label="Permalink to &quot;6) “If asked X, open Y” (fast routing)&quot;">​</a></h2><ul><li>Install / optional peers / CSS: <code>docs/guide/installation.md</code></li><li>Minimal usage: <code>docs/guide/quick-start.md</code>, <code>docs/guide/usage.md</code></li><li>Component catalog + requirements: <code>docs/guide/components.md</code></li><li>Props reference: <code>docs/guide/props.md</code></li><li>Streaming parser overview: <code>docs/guide/parser.md</code></li><li>Parser deep-dive: <code>docs/guide/parser-api.md</code></li><li>Performance knobs: <code>docs/guide/performance.md</code></li><li>Tailwind/reset ordering: <code>docs/guide/tailwind.md</code></li><li>Mermaid: <code>docs/guide/mermaid.md</code>, <code>docs/guide/mermaid-block-node.md</code></li><li>Math/KaTeX: <code>docs/guide/math.md</code></li><li>Monaco: <code>docs/guide/monaco.md</code>, <code>docs/guide/monaco-internals.md</code></li><li>VitePress integration: <code>docs/guide/vitepress-docs.md</code></li><li>Nuxt SSR: <code>docs/nuxt-ssr.md</code></li></ul><hr><h2 id="_6-1-intent-based-faq-routing-table-fast-answers" tabindex="-1">6.1) Intent-based FAQ routing table (fast answers) <a class="header-anchor" href="#_6-1-intent-based-faq-routing-table-fast-answers" aria-label="Permalink to &quot;6.1) Intent-based FAQ routing table (fast answers)&quot;">​</a></h2><p>Use this table as a “router”: map the user’s question to the most relevant docs/source, then answer with the minimal steps + the exact file/API names.</p><table tabindex="0"><thead><tr><th>Intent (what user wants)</th><th>User says (signals)</th><th>Fast answer checklist</th><th>Answer skeleton (copy/paste)</th><th>Minimal repro questions</th><th>Open docs first</th><th>Open code if needed</th></tr></thead><tbody><tr><td>Install + first render</td><td>“how to use”, “minimal example”</td><td>Import <code>markstream-vue/index.css</code> → render <code>&amp;lt;MarkdownRender :content=&quot;md&quot; /&amp;gt;</code></td><td>“Install <code>markstream-vue</code>, import <code>markstream-vue/index.css</code>, then render <code>&amp;lt;MarkdownRender :content=&quot;md&quot; /&amp;gt;</code>.”</td><td>“Vite/Nuxt? Which CSS reset? Any errors in console?”</td><td><code>docs/guide/quick-start.md</code>, <code>docs/guide/installation.md</code></td><td><code>src/exports.ts</code></td></tr><tr><td>Styles look wrong / missing</td><td>“CSS broken”, “unstyled”, “prose wrong”</td><td>Reset first → then <code>markstream-vue/index.css</code> (often <code>@layer components</code>) → if standalone node, wrap <code>.markstream-vue</code></td><td>“This is almost always CSS order: load your reset first, then <code>markstream-vue/index.css</code> (Tailwind: inside <code>@layer components</code>). If you render node components standalone, wrap them in <code>.markstream-vue</code>.”</td><td>“Show your CSS imports order (and Tailwind layers). Are you using standalone nodes or <code>MarkdownRender</code>?”</td><td><code>docs/guide/troubleshooting.md</code>, <code>docs/guide/tailwind.md</code></td><td><code>src/index.css</code></td></tr><tr><td>Tailwind conflicts</td><td>“Tailwind overrides”, “utilities win”</td><td>Put library CSS in <code>@layer components</code> → scope overrides with <code>custom-id</code> + <code>[data-custom-id=&quot;...&quot;]</code></td><td>“Put <code>@import &#39;markstream-vue/index.css&#39;</code> inside <code>@layer components</code> and scope your overrides with <code>custom-id</code> + <code>[data-custom-id=\\&quot;...\\&quot;]</code>.”</td><td>“Share the <code>main.css</code> (Tailwind layers) + the component usage with/without <code>custom-id</code>.”</td><td><code>docs/guide/tailwind.md</code>, <code>docs/guide/props.md</code></td><td><code>src/index.css</code></td></tr><tr><td>Streaming “stuck loading” at end</td><td>“final chunk”, “loading forever”, “unclosed fence/math”</td><td>On end-of-stream set <code>final: true</code> (ParseOptions or component prop)</td><td>“When your stream ends, set <code>final: true</code> (either <code>parseMarkdownToStructure(..., { final: true })</code> or <code>&amp;lt;MarkdownRender final /&amp;gt;</code>). This prevents mid-state ‘loading’ nodes from sticking.”</td><td>“Do you set <code>final</code> at end-of-stream? Provide the last ~40 chars of the markdown (often ends with ``` or $$).”</td><td><code>docs/guide/parser-api.md</code>, <code>docs/guide/parser.md</code></td><td><code>packages/markdown-parser/src/index.ts</code></td></tr><tr><td>Streaming feels jumpy / bursts</td><td>“typewriter”, “chunks dump”, “smooth stream”</td><td>Enable batching; tune <code>renderBatchSize</code>/<code>renderBatchDelay</code>; keep heavy nodes deferred</td><td>“Enable batching and tune <code>renderBatchSize</code>/<code>renderBatchDelay</code> for a steady flow; keep heavy nodes deferred (<code>viewportPriority</code>, <code>deferNodesUntilVisible</code>).”</td><td>“How often do you update <code>content/nodes</code> (per token? per chunk?) and what are your batch props?”</td><td><code>docs/guide/performance.md</code>, <code>docs/guide/props.md</code></td><td><code>src/components/NodeRenderer/NodeRenderer.vue</code></td></tr><tr><td>Long doc slow / memory high</td><td>“huge markdown”, “laggy scroll”</td><td>Use virtualization (<code>maxLiveNodes</code>, <code>liveNodeBuffer</code>) + defer heavy nodes</td><td>“Turn on/tune virtualization (<code>maxLiveNodes</code>, <code>liveNodeBuffer</code>) and keep heavy nodes deferred to avoid mounting everything.”</td><td>“Approx doc size? Are you passing <code>content</code> or <code>nodes</code>? Any huge code blocks/diagrams?”</td><td><code>docs/guide/performance.md</code></td><td><code>src/components/NodeRenderer/NodeRenderer.vue</code></td></tr><tr><td>Mermaid not rendering</td><td>“mermaid blank”, “diagram not shown”</td><td>Install <code>mermaid</code> peer → call <code>enableMermaid()</code> on client → check CSS/reset</td><td>“Install <code>mermaid</code>, call <code>enableMermaid()</code> on the client, then re-check CSS order/reset. Mermaid blocks won’t render if the peer isn’t installed/enabled.”</td><td>“Do you call <code>enableMermaid()</code> (where)? Any SSR? Does the markdown fence use ```mermaid?”</td><td><code>docs/guide/mermaid.md</code>, <code>docs/guide/troubleshooting.md</code></td><td><code>src/components/MermaidBlockNode/mermaid.ts</code></td></tr><tr><td>Mermaid in worker / via CDN</td><td>“no bundler”, “CDN worker”, “importScripts”</td><td>Use <code>createMermaidWorkerFromCDN()</code> + <code>setMermaidWorker()</code></td><td>“Use <code>createMermaidWorkerFromCDN(...)</code> and pass the worker to <code>setMermaidWorker(worker)</code> so Mermaid parsing happens in a worker without bundling.”</td><td>“Which worker mode (module/classic) and which CDN URL are you using? Any CSP restrictions?”</td><td><code>docs/guide/mermaid.md</code></td><td><code>src/workers/mermaidCdnWorker.ts</code>, <code>src/workers/mermaidWorkerClient.ts</code></td></tr><tr><td>KaTeX not rendering</td><td>“math not shown”, “katex missing”</td><td>Install <code>katex</code> peer + import <code>katex/dist/katex.min.css</code> → call <code>enableKatex()</code></td><td>“Install <code>katex</code>, import <code>katex/dist/katex.min.css</code>, then call <code>enableKatex()</code> on the client.”</td><td>“Do you import KaTeX CSS? Are you using <code>$$...$$</code> or <code>$...$</code>? Any SSR?”</td><td><code>docs/guide/math.md</code>, <code>docs/guide/installation.md</code></td><td><code>src/components/MathInlineNode/katex.ts</code></td></tr><tr><td>KaTeX in worker / via CDN</td><td>“katex worker”, “CDN worker”</td><td>Use <code>createKaTeXWorkerFromCDN()</code> + <code>setKaTeXWorker()</code></td><td>“Use <code>createKaTeXWorkerFromCDN(...)</code> and <code>setKaTeXWorker(worker)</code> to render math in a worker via CDN.”</td><td>“Which CDN URLs (katex + mhchem)? Worker mode? Is the page blocked by CSP?”</td><td><code>docs/guide/math.md</code></td><td><code>src/workers/katexCdnWorker.ts</code>, <code>src/workers/katexWorkerClient.ts</code></td></tr><tr><td>Monaco code blocks missing features</td><td>“toolbar missing”, “copy button missing”, “blank editor”</td><td>Install <code>stream-monaco</code> + import <code>stream-monaco/esm/index.css</code> → verify CSS order</td><td>“Install <code>stream-monaco</code> and import <code>stream-monaco/esm/index.css</code>. If the editor is blank, it’s usually missing Monaco CSS or CSS ordering issues.”</td><td>“Do you import <code>stream-monaco/esm/index.css</code>? Any SSR? Any console errors about workers?”</td><td><code>docs/guide/monaco.md</code>, <code>docs/guide/components.md</code></td><td><code>src/components/CodeBlockNode/</code></td></tr><tr><td>Prefer lightweight code blocks</td><td>“SSR friendly”, “reduce bundle”, “no monaco”</td><td>Use <code>MarkdownCodeBlockNode</code> (Shiki) or <code>render-code-blocks-as-pre</code></td><td>“Use <code>MarkdownCodeBlockNode</code> (Shiki) for lightweight highlighting, or set <code>render-code-blocks-as-pre</code> to force <code>&amp;lt;pre&amp;gt;&amp;lt;code&amp;gt;</code> rendering.”</td><td>“Do you need highlighting or just plain code? Are you OK installing <code>shiki</code> + <code>stream-markdown</code>?”</td><td><code>docs/guide/code-blocks.md</code>, <code>docs/guide/components.md</code></td><td><code>src/components/MarkdownCodeBlockNode/</code>, <code>src/components/PreCodeNode/</code></td></tr><tr><td>Custom components in Markdown</td><td>“embed Vue component”, “custom tag”, <code>&amp;lt;thinking&amp;gt;</code></td><td>Use <code>custom-html-tags</code> / <code>customHtmlTags</code> so parser emits nodes; map via <code>setCustomComponents</code></td><td>“Allow the tag via <code>custom-html-tags</code>/<code>customHtmlTags</code> so the parser emits a node type, then map it with <code>setCustomComponents(customId, { thinking: YourComp })</code>.”</td><td>“Do you control the markdown source? What tag name(s)? Do you want HTML passthrough or a custom node type?”</td><td><code>docs/guide/advanced.md</code>, <code>docs/guide/parser-api.md</code></td><td><code>src/utils/nodeComponents.ts</code>, <code>packages/markdown-parser/src/index.ts</code></td></tr><tr><td>SSR (Nuxt) issues</td><td>“window is not defined”, “SSR crash”</td><td>Wrap with <code>&amp;lt;client-only&amp;gt;</code>; keep heavy peers client-only; avoid server importing peers</td><td>“Wrap <code>&amp;lt;MarkdownRender&amp;gt;</code> with <code>&amp;lt;client-only&amp;gt;</code> in Nuxt, and ensure Mermaid/Monaco/worker setup runs only in the browser.”</td><td>“Nuxt version? Is the error during server build or runtime? Which peers are installed/enabled?”</td><td><code>docs/nuxt-ssr.md</code></td><td><code>playground-nuxt/</code> (patterns)</td></tr><tr><td>“What does package export?”</td><td>“is X exported”, “how to import Y”</td><td>Check <code>src/exports.ts</code> and package <code>exports</code> field</td><td>“Check <code>src/exports.ts</code> (re-exports) and <code>package.json#exports</code> for the correct import path.”</td><td>“Which symbol are you trying to import, and from which path?”</td><td><code>docs/guide/api.md</code>, <code>docs/guide/components.md</code></td><td><code>src/exports.ts</code>, <code>package.json</code></td></tr></tbody></table><h2 id="_7-commands-repo" tabindex="-1">7) Commands (repo) <a class="header-anchor" href="#_7-commands-repo" aria-label="Permalink to &quot;7) Commands (repo)&quot;">​</a></h2><ul><li>Playground dev: <code>pnpm dev</code></li><li>Docs dev/build/serve: <code>pnpm docs:dev</code>, <code>pnpm docs:build</code>, <code>pnpm docs:serve</code></li><li>Tests: <code>pnpm test</code> (Vitest)</li><li>Typecheck: <code>pnpm typecheck</code></li><li>Lint: <code>pnpm lint</code></li></ul><hr><h2 id="_8-quick-q→a-examples-what-good-answers-look-like" tabindex="-1">8) Quick Q→A examples (what good answers look like) <a class="header-anchor" href="#_8-quick-q→a-examples-what-good-answers-look-like" aria-label="Permalink to &quot;8) Quick Q→A examples (what good answers look like)&quot;">​</a></h2><p><strong>Q: Mermaid 为什么不显示？</strong></p><ul><li>Check peer installed (<code>mermaid</code>) + call <code>enableMermaid()</code> on client + verify CSS order/reset.</li><li>Open <code>docs/guide/mermaid.md</code> + <code>docs/guide/troubleshooting.md</code>.</li></ul><p><strong>Q: 流式渲染最后卡在 loading？</strong></p><ul><li>Use <code>final: true</code> / component prop <code>final</code> at end-of-stream.</li><li>Open <code>docs/guide/parser-api.md</code> (section about <code>final</code>) + <code>docs/guide/parser.md</code>.</li></ul><p><strong>Q: 代码块没有 Monaco 工具栏/按钮？</strong></p><ul><li>Ensure <code>stream-monaco</code> installed + <code>stream-monaco/esm/index.css</code> imported + check <code>docs/guide/monaco.md</code>.</li></ul>',59)])])}const h=o(a,[["render",s]]);export{p as __pageData,h as default};
