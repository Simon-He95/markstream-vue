name: Release (Stable)

on:
  push:
    tags:
      # Stable package tags: <pkg>@<semver>
      # e.g. markstream-vue@0.0.4, markstream-react@0.0.3-beta.2
      - '*@[0-9]*'

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-stable
  queue: max
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Parse tag
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF_NAME}"
          PKG="${TAG%@*}"
          VERSION="${TAG##*@}"

          if [[ -z "${PKG}" || -z "${VERSION}" ]]; then
            echo "Invalid tag: ${TAG}"
            exit 1
          fi

          PRERELEASE=false
          if [[ "${VERSION}" == *-* ]]; then
            PRERELEASE=true
          fi

          echo "pkg=${PKG}" >> "${GITHUB_OUTPUT}"
          echo "version=${VERSION}" >> "${GITHUB_OUTPUT}"
          echo "prerelease=${PRERELEASE}" >> "${GITHUB_OUTPUT}"
          echo "name=[${PKG}] ${VERSION}" >> "${GITHUB_OUTPUT}"

      - name: Checkout (for changelog body)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run release verification
        if: ${{ steps.meta.outputs.pkg == 'markstream-vue' }}
        run: pnpm release:verify

      - name: Build package for npm
        if: ${{ steps.meta.outputs.pkg != 'markstream-vue' }}
        env:
          PACKAGE_NAME: ${{ steps.meta.outputs.pkg }}
        shell: bash
        run: |
          set -euo pipefail

          case "${PACKAGE_NAME}" in
            stream-markdown-parser) PACKAGE_DIR="packages/markdown-parser" ;;
            markstream-core) PACKAGE_DIR="packages/markstream-core" ;;
            markstream-angular) PACKAGE_DIR="packages/markstream-angular" ;;
            markstream-octane) PACKAGE_DIR="packages/markstream-octane" ;;
            markstream-react) PACKAGE_DIR="packages/markstream-react" ;;
            markstream-svelte) PACKAGE_DIR="packages/markstream-svelte" ;;
            markstream-vue2) PACKAGE_DIR="packages/markstream-vue2" ;;
            *) echo "Unknown package: ${PACKAGE_NAME}" >&2; exit 1 ;;
          esac

          pnpm --dir "${PACKAGE_DIR}" run build

      - name: Publish package to npm
        env:
          PACKAGE_NAME: ${{ steps.meta.outputs.pkg }}
          PACKAGE_VERSION: ${{ steps.meta.outputs.version }}
        shell: bash
        run: |
          set -euo pipefail

          case "${PACKAGE_NAME}" in
            markstream-vue) PACKAGE_DIR="." ;;
            stream-markdown-parser) PACKAGE_DIR="packages/markdown-parser" ;;
            markstream-core) PACKAGE_DIR="packages/markstream-core" ;;
            markstream-angular) PACKAGE_DIR="packages/markstream-angular" ;;
            markstream-octane) PACKAGE_DIR="packages/markstream-octane" ;;
            markstream-react) PACKAGE_DIR="packages/markstream-react" ;;
            markstream-svelte) PACKAGE_DIR="packages/markstream-svelte" ;;
            markstream-vue2) PACKAGE_DIR="packages/markstream-vue2" ;;
            *) echo "Unknown package: ${PACKAGE_NAME}" >&2; exit 1 ;;
          esac

          if npm view "${PACKAGE_NAME}@${PACKAGE_VERSION}" version >/dev/null 2>&1; then
            echo "${PACKAGE_NAME}@${PACKAGE_VERSION} is already published; skipping."
            exit 0
          fi

          DIST_TAG="$(node scripts/resolve-dist-tag.mjs "${PACKAGE_NAME}" "${PACKAGE_VERSION}")"
          TARBALL="${RUNNER_TEMP}/${PACKAGE_NAME}-${PACKAGE_VERSION}.tgz"
          pnpm --dir "${PACKAGE_DIR}" pack --pack-destination "${RUNNER_TEMP}"
          npm publish "${TARBALL}" --access public --tag "${DIST_TAG}"

      - name: Build changelog body (best effort)
        id: changelog
        if: ${{ steps.meta.outputs.pkg == 'markstream-vue' }}
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${{ steps.meta.outputs.version }}"
          BODY_FILE="$(mktemp)"
          FOUND=0

          awk -v version="${VERSION}" '
            BEGIN { capture = 0; found = 0 }
            /^## \[/ {
              if (capture == 1)
                exit
              if ($0 ~ "^## \\[" version "\\]") {
                capture = 1
                found = 1
                next
              }
            }
            capture == 1 { print }
            END {
              if (found == 0)
                exit 2
            }
          ' CHANGELOG.md > "${BODY_FILE}" && FOUND=1 || true

          if [[ "${FOUND}" == "1" && -s "${BODY_FILE}" ]]; then
            echo "has_body=true" >> "${GITHUB_OUTPUT}"
            echo "body_path=${BODY_FILE}" >> "${GITHUB_OUTPUT}"
          else
            echo "has_body=false" >> "${GITHUB_OUTPUT}"
            echo "::warning::Unable to find CHANGELOG.md section for version ${VERSION}; falling back to auto-generated release notes"
          fi

      - name: Create GitHub Release (markstream-vue with changelog)
        if: ${{ steps.meta.outputs.pkg == 'markstream-vue' && steps.changelog.outputs.has_body == 'true' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ steps.meta.outputs.name }}
          prerelease: ${{ steps.meta.outputs.prerelease }}
          generate_release_notes: true
          body_path: ${{ steps.changelog.outputs.body_path }}
          append_body: true

      - name: Create GitHub Release (markstream-vue fallback)
        if: ${{ steps.meta.outputs.pkg == 'markstream-vue' && steps.changelog.outputs.has_body != 'true' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ steps.meta.outputs.name }}
          prerelease: ${{ steps.meta.outputs.prerelease }}
          generate_release_notes: true

      - name: Create GitHub Release (other packages)
        if: ${{ steps.meta.outputs.pkg != 'markstream-vue' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ steps.meta.outputs.name }}
          prerelease: ${{ steps.meta.outputs.prerelease }}
          generate_release_notes: true
